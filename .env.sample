# RemoteVote NG Frontend Environment Variables

# Base URL pointing to the Django REST API backend
# For development, this usually defaults to http://localhost:8000/api
# For production/deployment, replace with your hosted backend URL (e.g. https://your-backend.vercel.app/api)
VITE_API_BASE_URL=
